# Step 1: Update package.json
# Replace your current package.json build scripts and dependencies

# BEFORE (Vite):
# "scripts": {
#   "build": "vite build",
#   "dev": "vite dev"
# }
# "devDependencies": {
#   "vite": "^5.4.19"
# }

# AFTER (Next.js):
cat > package.json << 'EOF'
{
  "name": "facility-management-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "next": "^14.0.0",
    "recharts": "^2.8.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.0.0"
  }
}
